// @flow
import React from 'react';

import { wrapMainLayout } from 'components/layouts';

export const AddChildPage = (props: PropsType) => {
  return (
    <div>
      Hello
    </div>
  );
};

export default wrapMainLayout(AddChildPage);
